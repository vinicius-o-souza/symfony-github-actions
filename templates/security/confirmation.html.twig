{% extends "base.html.twig" %}

{% block body %}
    {% if user %}
        <h5>Congratulations man!</h5>
        <p>Your account is confirmed! You can now sign in
            <a href="{{ path('security_login') }}">here</a>
        </p>
    {% else %}
        <p>Deu ruim patrão!</p>
    {% endif %}
{% endblock %}